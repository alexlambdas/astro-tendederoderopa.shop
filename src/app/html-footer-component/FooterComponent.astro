---

import { fnGetConfigAppObj, fnGetStateAppObj } from '../InitApp';
import CommonModule from '../server-common-module/Common.module';

const configAppObj = fnGetConfigAppObj();
const domainName = configAppObj.SITE_DOMAIN_LINKS;

interface Props {
  page: string,
}

const { page } = Astro.props;

const fnFilterMetaTagByPage = CommonModule().fnFilterMetaTagByPage;
const stateAppObj = fnGetStateAppObj();
const metaTagObj = stateAppObj.metaTags;
const metaTagFiltered = fnFilterMetaTagByPage(page)(metaTagObj);

---

<footer class="flex flex-col bg-zinc-900 pt-4 mt-4">
  <div class="flex flex-col m-auto w-11/12 lg:flex-row lg:justify-center">
    <section class="font-sans text-white text-2xl font-medium mt-4 mb-4 lg:mt-10 lg:w-1/4">
      <p class="flex justify-end lg:justify-start">
        <a href={domainName} class="text-yellow-400 underline underline-offset-1">
          TendederoDeRopa.shop
        </a>
      </p>
      <p class="font-mono text-white text-sm mt-6 lg:pr-7">
        {metaTagFiltered.data[0].attributes.title}
      </p>
    </section>
    <section class="mt-12 lg:w-1/4">
      <p class="text-white font-sans mb-6">Pago Seguro a través de Amazon</p>
      <div class="grid grid-cols-4 items-center mt-4">
        <div>
          <img 
            src={domainName+'/footer/visa_64x64.png'} 
            alt="pago seguro visa"
          >
        </div>
        <div>
          <img 
            src={domainName+'/footer/card_64x64.png'} 
            alt="pago seguro mastercard"
          >
        </div>
        <div>
          <img 
            src={domainName+'/footer/dinners-club_64x64.png'} 
            alt="pago seguro dinners club"
          >
        </div>
        <div>
          <img 
            src={domainName+'/footer/american-express_64x64.png'} 
            alt="pago seguro american express"
          >
        </div>
      </div>
      <p class="font-mono text-gray-400 text-xs mt-4">
        Incluimos productos que creemos que son útiles para nuestros lectores. Si compra a través 
        de enlaces en esta página, podemos ganar una pequeña comisión.
      </p>
    </section>
    <section class="mt-12 lg:w-1/4">
      <div class="flex flex-col lg:w-2/3 m-auto">
        <p class="font-sans text-white mb-6">Legal</p>
        <ul class="grid grid-cols-1 font-mono text-gray-400 text-sm">
          <li class="hover:text-yellow-400 transition duration-300 mb-4">
            <a  class="underline underline-offset-1" href={domainName+'/aviso-legal'}>Aviso Legal</a>
          </li>
          <li class="hover:text-yellow-400 transition duration-300 mb-4">
            <a class="underline underline-offset-1" href={domainName+'/politica-de-cookies'}>Política de Cookies</a>
          </li>
          <li class="hover:text-yellow-400 transition duration-300 mb-4">
            <a class="underline underline-offset-1" href={domainName+'/politica-de-privacidad'}>Política de Privacidad</a>
          </li>
          <li class="hover:text-yellow-400 transition duration-300 mb-4">
            <a class="underline underline-offset-1" href={domainName+'/unsplash'}>Unsplash</a>
          </li>
          <li class="hover:text-yellow-400 transition duration-300 mb-4">
            <a class="underline underline-offset-1" href={domainName+'/flaticons'}>Flaticons</a>
          </li>
        </ul>
      </div>
    </section>
    <section class="mt-12 mb-12 lg:w-1/4">
      <p class="text-white font-sans mb-6">Acerca de Nosotros</p>
      <p class="text-gray-400 mt-2 text-sm">
        Somos <span class="text-white">@TrulyDigital</span>, una empresa dedicada a soluciones 
        tecnológicas, desarrollo de software y posicionamiento SEO de sitios web
      </p>
      <div class="pb-6 mt-10 flex">
        <a href={domainName+'/contacto'} class="pr-4 pl-4 pt-2 pb-2 bg-yellow-400 text-zinc-900 text-xs font-medium hover:bg-white hover:text-zinc-900 transition duration-300">
          Contáctanos
        </a>
      </div>
    </section>
  </div>
  <div class="h-7 w-full bg-zinc-800"></div>
</footer>